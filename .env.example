# Port du serveur (Render utilise automatiquement son propre port)
PORT=3000

# Environnement (development ou production)
NODE_ENV=production

# 🔑 SÉCURITÉ - CHANGEZ CES VALEURS !
# Clé API pour authentifier les requêtes
API_KEY=CHANGEZ_MOI_ICI

# Secret JWT pour générer les tokens
JWT_SECRET=CHANGEZ_MOI_AUSSI

# Exiger une API Key pour toutes les requêtes (true ou false)
REQUIRE_API_KEY=true

# CORS - Origins autorisées (* pour tout autoriser)
ALLOWED_ORIGINS=*

# LOGS
LOG_LEVEL=info
LOG_RETENTION_DAYS=7

# COMMANDES
COMMAND_EXPIRATION_HOURS=1
MAX_PENDING_COMMANDS=100
